name: Build Sprite Scout

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet restore FortniteSpriteTracker.sln
      - run: dotnet build FortniteSpriteTracker.sln -c Release --no-restore
      - run: dotnet publish src/FortniteSpriteTracker.Server/FortniteSpriteTracker.Server.csproj -c Release --no-build
      - uses: actions/upload-artifact@v4
        with:
          name: sprite-scout-server
          path: src/FortniteSpriteTracker.Server/bin/Release/net10.0/publish
